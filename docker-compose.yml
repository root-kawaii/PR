services:
  postgres:
    image: postgres:16-alpine
    container_name: pierre-local-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: pierre_local
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d pierre_local"]
      interval: 5s
      timeout: 5s
      retries: 20
    volumes:
      - pierre_postgres_data:/var/lib/postgresql/data

volumes:
  pierre_postgres_data:
