version: '3.8'

services:
  postgres:
    image: postgres:16
    container_name: postgres-dev-pierre
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_DB: events
    ports:
      - "5433:5433"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data: